services:
  - type: web
    name: celtm-backend
    env: python
    rootDir: backend
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python scripts/migrate_runtime.py && uvicorn app.main:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
